# DnD Tracker — статус проекта

Обновлено: 31 июля 2026

## Что это

Трекер персонажей и кампаний D&D. Существует в двух видах:

| Клиент | Где | Состояние |
|---|---|---|
| Android (Kotlin + Compose + Room) | `android/` | Работает, локальный, однопользовательский. В Play Console — closed testing, versionCode 18 |
| Веб (React + TS + Supabase) | `web/` | **В разработке**, начат 31.07.2026 |

Исходники Android-версии выгружены из Google AI Studio
(`https://ai.studio/apps/ddbf0f5a-3824-43b5-8b5d-7abad30729bf`) 31.07.2026.
GitHub-репозиторий `yevhop-pixel/dnd-party-tracker` пустой — кода там нет.

## Задача текущего этапа

Веб-версия с аккаунтами и многопользовательской игрой. Требования владельца:

1. Сайт, регистрация под своим аккаунтом, весь функционал Android-версии.
2. Отдельная роль гейммастера:
   - видит листы всех игроков кампании целиком;
   - заводит карты локаций (разные карты под разные локации);
   - видит все броски кубов; игроки тоже видят броски друг друга;
   - личная переписка ГМ ↔ каждый игрок в обе стороны;
   - игроки могут приближать карту.

## Принятые решения

**Стек веба:** React + TypeScript + Vite, mobile-first.
Причина: Android-версия — mobile-first, играют с телефонов; Vite даёт быструю сборку
и статику, которую можно раздать откуда угодно.

**Бэкенд: Supabase** (Postgres + Auth + Realtime + Storage), а не Firebase.
Причина: данные Android-версии уже реляционные (9 таблиц Room) и ложатся в Postgres
один в один. Главное — Row Level Security: правило «ГМ видит все листы своей кампании,
игрок только свой» описывается декларативно в базе, а не проверками в коде клиента.
На Firestore это же правило разъехалось бы по правилам безопасности и коду.

**Карты: Leaflet в режиме `CRS.Simple`.** Приближение, перетаскивание, инерция на
тач-экранах — из коробки, без своей математики жестов.

**Разграничение прав — только через RLS.** Клиент ходит в базу напрямую с anon-ключом,
поэтому единственная настоящая граница безопасности — политики в `supabase/schema.sql`.
Ничего не проверять «на фронте и достаточно».

**Android-версия пока остаётся как есть.** Она локальная и однопользовательская;
переделывать её под общий бэкенд — отдельная задача после того, как веб заработает.
Схема веба намеренно повторяет её модель данных, чтобы это было возможно.

**Броски не редактируются и не удаляются** — лента бросков это протокол игры.
Добавлен «тайный бросок» (`is_secret`): виден только автору и ГМу.

**Сохранки: облако = Supabase (автосохранение каждой правки), локально = экспорт/импорт
JSON-файла.** Формат импорта совместим с экспортом Android-версии — перенос персонажа
с телефона файлом. Google Drive в приложение не встраиваем (владелец предлагал — решено
отказаться: отдельный OAuth-процесс ради дублирования Supabase + локального файла).

## Структура

```
Tracker/
  android/          Android-приложение (выгрузка из AI Studio, не трогать без нужды)
  web/              веб-клиент          — ещё не создан
  supabase/
    schema.sql      схема БД + RLS + realtime + storage  — ГОТОВО
  docs/
  STATUS.md
```

## Сделано

- [x] Исходники Android выгружены и разобраны
- [x] Проект реструктурирован под два клиента
- [x] Спроектирована схема БД: 14 таблиц, RLS-политики, realtime, bucket для карт
      → `supabase/schema.sql`

## Дальше

- [ ] **Нужен проект Supabase** (создаёт владелец — см. ниже), выполнить в нём `schema.sql`
- [x] Скаффолд `web/`: Vite + React + TS, подключение supabase-js, роутинг, тёмная тема
- [x] Авторизация: регистрация и вход (проверено сборкой; против живого Supabase ещё не гонялось)
- [x] Создание кампании / вход по коду (RPC `join_campaign_by_code` в schema.sql) + API-слой `web/src/lib/api.ts`
- [x] Редактор листа: каркас вкладок, автосохранение (debounce + flush при уходе + обработка
      ошибки сети), вкладки Статы и Заметки
- [x] Все 9 вкладок листа: Статы, Черты, Инвентарь (итоги веса/стоимости), Эквип (11 слотов),
      NPC (фильтр по отношению), Квесты (статусы кнопками), Зелья, Расходники, Заметки
- [x] Экспорт листа в JSON-файл + импорт (свой формат и Android-экспорт — перенос с телефона)
- [x] Код-ревью слоя листа: 3 critical (эскалация до ГМа через member_join; createCampaign
      падал об RLS при insert+RETURNING; listMyCampaigns без фильтра user_id), 8 major,
      14 minor. Полный отчёт — в истории сессии
- [x] Все находки ревью починены (3 critical, 8 major, 14 minor — оба потока, сборка чистая).
      Ключевые решения: create_campaign как SECURITY DEFINER RPC; npc/quest получили
      created_at для детерминированного порядка; перемещение — полная перенумерация
      sort_order вместо парного обмена (самопочинка дубликатов); общий хук
      useDebouncedPatches с flush на pagehide/unmount; импорт откатывается целиком
- [x] Модули dice/chat/maps готовы (src/features/*). Решения: секретные броски фильтрует
      КОМПОНЕНТ (realtime-payload не проходит RLS!); у игроков видна максимум одна
      открытая карта (reveal автоскрывает остальные); приватный бакет + signed URL 60 мин;
      «Оба куба» = две записи в ленте; setRevealed принимает campaignId третьим параметром
- [x] GmView (игроки с полным листом read-only, кубы, карты, чат, код приглашения)
      и PlayerView (привязка персонажа, лист/кубы/карта/чат) собраны; SheetReadOnly;
      api: getCampaign, listCampaignMembers, listCampaignSheets, detachSheetFromCampaign.
      ВЕСЬ заявленный функционал написан
- [x] Ревью многопользовательской волны (2 critical, 7 major, 10 minor) и починка всего.
      Ключевое: политика member_join УДАЛЕНА (вступление только через RPC — иначе код
      кампании декоративен); map_file_read привязан к is_revealed; reveal_map RPC;
      replica identity full для game_map (иначе DELETE-события не доходят)
- [x] Supabase живой: проект plgmhzlzvtohtrtclrhe (яндекс: НЕ трогать руками — вся схема
      в schema.sql), схема выполнена, Confirm email ОТКЛЮЧЁН, web/.env создан
- [x] Сквозной прогон вживую: 2 аккаунта, кампания «Проклятие Страда (тест)» код NBBPAT,
      вход по коду, лист у ГМа, общая лента бросков, чат в обе стороны с непрочитанными.
      Не прогнано руками: загрузка карты (file input не автоматизируется во встроенном
      браузере), realtime-пуш при двух одновременных сессиях, экспорт/импорт
- [x] Кубы: анимация ~2с (бросок в базу уходит сразу — протокол не задерживается),
      честный рандом crypto.getRandomValues + rejection sampling, prefers-reduced-motion
- [x] Фото в чате: message.attachment_path + приватный бакет chat-files (файл читает
      только тот, кому видно сообщение), 📎 с превью, миграция применена к живой базе
- [x] **ЗАДЕПЛОЕНО: https://yevhop-pixel.github.io/dnd-party-tracker/** — GitHub Actions
      → Pages при пуше в main; вход на проде проверен. Publishable-ключ в workflow
      открыто — осознанно, ключ публичный

## Продакшн

- Сайт: https://yevhop-pixel.github.io/dnd-party-tracker/
- База: Supabase, проект plgmhzlzvtohtrtclrhe (аккаунт yevhop@gmail.com), Free план.
  Схема НЕ правится руками в дашборде — только через supabase/schema.sql (идемпотентен).
  Confirm email отключён осознанно.
- Тестовые аккаунты: dnd.tracker.gm.test@gmail.com / dnd.tracker.player.test@gmail.com
  (пароль Test-Pass-2026!), тестовая кампания «Проклятие Страда (тест)», код NBBPAT —
  перед раздачей сайта игрокам можно удалить (Auth → Users; кампания снесётся каскадом).

## Сессия автотестов 31.07 вечер (владелец делегировал: «тести всё сам»)

Прогнано на двух ЖИВЫХ сессиях одновременно (ГМ на localhost-dev, игрок на проде,
одна база) — realtime проверен по-настоящему:

- [x] Лента бросков: бросок ГМа долетает игроку живьём без перезагрузки
- [x] Тайный бросок: у ГМа в ленте с пометкой «тайный», у игрока НЕ появляется
- [x] Чат: сообщение ГМа приходит игроку в открытый диалог мгновенно
- [x] Карта: «Показать игрокам» долетает живьём; «Скрыть» — ТОЖЕ (см. фикс ниже)
- [x] Карты через UI: загрузка файла, список, статусы, зум, вид игрока (прод)
- [x] Редактор листа: профиль/ХП/статы, черта (добавление + инлайн-правка),
      инвентарь (итоги веса/стоимости), зелья (+1), заметки — всё пережило F5
- [x] Инлайн-формы создания персонажа (вместо window.prompt) — работают
- [x] Кнопка «Копировать код»: фолбэк + видимая обратная связь

**Найден и починен критичный realtime-баг:** событие «карту скрыли» не доставлялось
игроку — после is_revealed=false строка game_map перестаёт проходить его RLS-чтение
и push молча выпадает. Решение: таблица campaign_state (читаема участникам всегда),
reveal_map/hide_map обновляют её, PlayerMap подписан на неё. Урок для будущих фич:
**realtime-событие не доходит, если изменение делает строку невидимой подписчику» —
для таких переходов нужен всегда-видимый сигнальный ряд.**

Разовые аномалии (не воспроизведены повторно, следить):
- одна потеря сессии на localhost-dev (перелогин помог; на проде не наблюдалась)

## Мандат владельца (01.08): «делай все» — фичи по списку

Очередь (сверху вниз), реализовано ✅ / в работе 🔄 / не начато ⬜:
1. ✅ Аватарки персонажей (загрузка в TabStats, показ везде; бакет avatars)
2. ✅ Броски из листа (тап по характеристике/инициативе = d20+мод в ленту; смоук пройден)
3. ✅ PWA (манифест, SVG-иконка d20, apple-touch-icon; на реальном телефоне не проверено)
4. ✅ Макросы бросков (MacroBar во вкладке «Кубы» игрока; рантайм не проверен)
5. ✅ Трекер инициативы (вкладка «Бой» у ГМа и игрока; realtime по урокам; рантайм не проверен)
6. 🔄 Токены на карте (таблица map_token готова, replica identity full; клиент в работе)
7. ⬜ Туман войны
8. 🔄 Приватные заметки ГМа (таблица gm_note готова; клиент в работе)
9. ⬜ Журнал кампании
10. ⬜ SRD-справочник заклинаний/предметов
11. ⬜ Push-уведомления
12. ⬜ Общий чат партии

**Урок с аватарками (стоил часа отладки):** в RLS-политиках storage.objects
НЕЛЬЗЯ писать неквалифицированное `name`, если в подзапросе таблица со своей
колонкой name (character_sheet!) — захват внутренней областью, политика молча
ложна. Всегда `storage.objects.name`. И: «Success» в SQL Editor может быть
СТАРЫМ — после Run с drop/alter всегда ждать диалог подтверждения и проверять
результат отдельным select (pg_policy / pg_get_expr).

## Хвосты (не блокеры)

- [ ] Не прогнано руками: экспорт/импорт файлом через UI (логика на ревью прошла),
      мобильная вёрстка вживую, IME/тач-поведение чата
- [ ] Бандл 689 КБ — при желании порезать code-splitting'ом (leaflet тянется всюду)
- [ ] В ленте бросков имя «Игрок» если участник не подгрузился — редкий случай
- [ ] Android-версия не знает про веб: общий бэкенд для неё — отдельный этап
- [ ] В тестовой кампании остались тестовые данные (Готрик, Мордекай, таверна,
      броски, переписка) — это песочница, можно снести целиком удалением кампании
- [ ] Отметить: бандл вырос 232→495 КБ после волны починок (вероятно realtime-часть
      supabase-js) — проверить при оптимизации, не блокер
- [ ] Перенос 11 вкладок листа персонажа из Compose в React
- [ ] Экран ГМа: листы всех игроков
- [ ] Общая лента бросков (realtime)
- [ ] Карты: загрузка ГМом, Leaflet-просмотр с зумом, показ/скрытие
- [ ] Переписка ГМ ↔ игрок (realtime)
- [ ] Деплой

## Что нужно от владельца

Аккаунты я заводить не могу — это делает владелец:

1. Создать проект на https://supabase.com (бесплатного тарифа хватает на группу игроков).
2. Прислать **Project URL** и **anon public key** (Settings → API).
   `service_role` ключ не нужен и присылать его не надо.
3. В SQL Editor выполнить `supabase/schema.sql`.

## Грабли, о которых надо помнить

- **Проект лежит в OneDrive.** `node_modules` в веб-клиенте — это десятки тысяч файлов,
  OneDrive будет их синхронизировать. Папка исключена в `.gitignore`, но синхронизацию
  стоит отключить для `web/node_modules` вручную.
- **Android-сборка «как есть» не собирается**: в выгрузке нет ни `debug.keystore`,
  ни `my-upload-key.jks`, хотя `android/app/build.gradle.kts` на них ссылается.
  Перед локальной сборкой убрать строку `signingConfig = signingConfigs.getByName("debugConfig")`.
- **AI Studio — отдельная ветка истории.** Повторный Export оттуда перезатрёт локальные
  правки в `android/`. Публикация сборки не из AI Studio потребует сброса upload key
  в Play Console.
- В Play Console висят 7 приложений — итерации одного трекера. Живое только V1.5
  (`com.aistudio.rpgtracker.hrygqb`), остальные шесть Draft. Есть непрочитанные
  уведомления Google — не разбирались.
